# PRODUCT_ROADMAP.md — источник правды

Общая память между сессиями. Каждая автономная сессия **начинается** с чтения
этого файла и **заканчивается** записью в журнал внизу.

Технические правила репозитория — в `CLAUDE.md`. Здесь — что мы делаем и зачем.

---

## 0. Куда мы идём

«АвтоПост» должен быть сервисом, которым **не стыдно вести свой канал**.
Не «неплохо для ИИ», а так, чтобы владелец нажимал «Опубликовать» без
внутреннего сопротивления.

Расписание, очередь, интеграция с Telegram — это обвязка, её повторит любой.
Единственное настоящее отличие от «просто открой ChatGPT и скопируй» —
**качество текста и попадание в стиль канала**. Всё, что не служит этому,
второстепенно.

Отдельная цель по интерфейсу: он должен быть **лаконичным и понятным без
объяснений**. Пользователь видит то, что нужно сейчас, и не тонет в
информации. Аналогов такого уровня в русскоязычном сегменте нет — это
достижимое конкурентное преимущество.

---

## 1. Принципы

**Если задача противоречит принципу — неверна задача, а не принцип.**

1. **Интерфейс не обещает того, чего система не делает.** Прежде чем написать
   обещание, найди код, который его выполняет. Мы уже обещали таймер постам
   без таймера, «очередь на неделю», которая не собиралась, и автогенерацию
   на каналах, где она не запускается.

2. **Проверять результат, а не код.** Дубли постов были невидимы при чтении
   исходников: запрет в промпте стоял, дедупликация работала — сравнивала
   заголовки. Нашлось только когда два реальных поста легли рядом. Любая
   правка качества проверяется на реальном выводе.

3. **Ничего не уходит в канал молча.** Только автопилот, явное нажатие или
   показанный таймер. Это то, чем мы отличаемся от «страшной автоматизации».

4. **Пользователь не должен догадываться.** Если после действия ничего не
   происходит — интерфейс обязан сказать, что происходит и сколько ждать.
   Не «Загрузка…», а «Готовим первые посты, одна-две минуты».

5. **Одно изменение за раз, с проверкой.** Не смешивать в одной задаче фикс,
   рефактор и новую фичу. Иначе непонятно, что именно сработало.

6. **Работает из России без VPN.** У отрисовки нет внешних зависимостей.
   Проверено дорого: Google Fonts держал страницу белой 25 секунд у всего
   рекламного трафика.

7. **Деньги — с максимальной осторожностью.** Идемпотентность, защита от
   двойного списания, зафиксированная цена, отвязка карты пользователем.
   Неизвестный исход платежа — не то же самое, что отказ.

8. **Приватность по умолчанию.** Внутренние ручки за токеном, наружу — не
   больше, чем нужно для задачи. Тексты постов — только владельцу сервиса.

9. **Честность данных.** Не выдавать оценку за факт. Если цифра посчитана при
   допущении — допущение называется рядом. Мы уже пять раз завышали
   себестоимость, считая внутренние единицы учёта провайдерскими токенами.

10. **Решение о продукте принимает владелец.** Автономная сессия готовит
    вариант, аргументирует и делает — но не меняет цены, позиционирование и
    юридические условия по собственной инициативе.

---

## 2. Бэклог

Порядок значим: сначала сломанное, потом понятность, потом ценность.
Внутри блока задачи независимы — можно брать любую.

### A. Сломанное и блокирующее

- [ ] **A1. Деплой.** Timeweb не клонирует репозиторий с GitHub (таймаут к
      `github.com:443`). Пока не решено — ничего из сделанного не доезжает до
      пользователей. Проверить, повторяется ли; если да — обращение в
      поддержку с формулировкой «таймаут TCP, не ошибка авторизации».
- [ ] **A2. Reasoning-токены DeepSeek.** Найти в логах прода строку
      `[llm-usage]`. Если `reasoning` в тысячах — `thinking: disabled` не
      работает, и себестоимость выше расчётной. Чинить до любых решений о ценах.
- [x] **A3. Тесты не запускаются.** Сделано 27.07: `conftest.py`, 63 теста
      проходят, проверено мутациями. Подробности в журнале.
- [ ] **A4. Автоплатежи ЮKassa.** На согласовании. После подключения —
      `SUBSCRIPTION_ENABLED=true` и проверка полного цикла продления.
- [x] **A5. Тестов нет там, где дороже всего ошибиться.** Закрыто 27.07:
      удаление аккаунта (`test_account_deletion.py`), автосписание
      (`test_subscription_billing.py`) и дедупликация постов
      (`test_duplicate_posts.py`). Всего 88 тестов.

- [x] **A6. Пять тестов ничего не проверяют.** Исправлено 27.07: настоящие
      `assert` и честный пропуск без ключей. Доказано сравнением: старая
      версия давала 5 passed без ключей, новая — 5 failed.

### B. Понятность интерфейса (стартовый блок)

- [x] **B1. Пустые состояния везде.** Проверено 27.07 на трёх состояниях
      аккаунта: пустые экраны объясняют почему пусто, что дальше и что
      сделать сейчас. Дефектов в самих пустых состояниях не нашлось —
      подробности в журнале.
- [x] **B2. Первый вход.** Обе ветки пройдены с чистого аккаунта. В первой
      сбой ИИ выдавался за плохую тему, во второй «проанализируем ваш канал»
      ничего не анализировало и подставляло @username как тему. Оба
      исправлены. Не проверен только успешный путь целиком — без ключей
      модели он здесь не воспроизводится.
- [x] **B3. Объяснить каждое число.** Экран тарифов проверен 27.07 — числа
      там объяснены рядом (диапазон постов, токены в реферальной программе,
      «0 из 3» в очереди). Вкладки «Расширенные» и «Настройки» проверены
      27.07 — там нашлось не про числа, а про смысл: «Окно публикации»
      ограничивало написание, а не публикацию, а «тестовый пост» оказался
      обычным постом за токены. История платежей проверена 27.07: показывала
      токены и скрывала сумму — исправлено.
- [x] **B4. Мобильная вёрстка.** Замерено на 390px: горизонтального
      переполнения и обрезанного текста нет нигде. Починены два дефекта:
      плашка cookies закрывала юридические ссылки, кнопка «⋯» была 32×27
      рядом с «Опубликовать сейчас».
- [x] **B13. Ссылки в подвале мелковаты для пальца.** Исправлено 28.07 в
      обоих подвалах — кабинета и лендинга. Было 44×19, 185×19, 48×19 и
      48×20, 201×20, 52×20; стало 45px и 44px по высоте при том же положении
      надписей. Проверено попаданием в четыре точки каждой области и
      настоящими тапами.
- [ ] **B5. Единый словарь.** Обращение к пользователю приведено к «вы»:
      27.07 в три захода: первый шёл по списку слов и пропустил шесть мест,
      второй искал только формы с заглавной буквы и пропустил ещё четыре.
      Рабочая проверка — по основам повелительных форм в любом регистре и
      любой позиции строки.
      Термины проверены 27.07. «Отклонить» и «Удалить» оказались разными
      действиями и названы верно; «Telegram-канал» в кабинете единообразен.
      Найдено и исправлено настоящее: один и тот же параметр назывался тремя
      именами. Владелец решил писать «Телеграм» кириллицей — приведено 27.07
      к этому виду везде, кроме оферты (юридический документ, менять без
      владельца нельзя).
- [x] **B8. Дубль на первом экране онбординга.** Исправлено 27.07 вместе со
      второй такой же находкой — двумя кнопками «Пропустить» на экране
      выбора.
- [ ] **B6. Убрать дублирующие сообщения.** Одна мысль — в одном месте.
      Раньше «канал не подключён» было написано трижды на одном экране.
- [x] **B9. Срок генерации написан дважды.** Исправлено 27.07: строка убрана
      из шапки экрана канала, на дашборде осталась.
- [x] **B10. «Сам не опубликуется» сказано четырьмя способами.** 27.07:
      заголовок блока очереди заменён на счётчик, повторов стало на один
      меньше. Оставшиеся три работают по-разному — подробности в журнале.
- [x] **B7. Уменьшить плотность.** Замерено числом: первый пост начинался на
      612px из 844. Убрали объяснение механики с непустой очереди — его и так
      говорит каждая карточка. Стало 571px, блок очереди 163→121px.
- [ ] **B14. Шапка канала занимает 208px.** Аватар, название, @username,
      статус, тема канала и две кнопки — на каждом заходе. Тема канала не
      меняется и редактируется в настройках; её можно убрать с рабочего
      экрана. Это уже решение о вёрстке, а не устранение повтора.

### C. Ценность продукта (то, за что платят)

- [ ] **C1. Оценить качество постов честно.** Прочитать 20 реальных постов
      подряд и разметить: опубликовал бы / нет / почему. Это единственный
      способ понять, есть ли продукт вообще.
- [ ] **C2. Проверить попадание в стиль.** `analyze_style` копирует стиль
      чужого канала — работает ли это заметно. Сравнить пост «со стилем» и
      «без» на одной теме.
- [ ] **C3. Мониторинг дублей.** После деплоя смотреть в логах
      `пост дублирует уже существующий (совпадение N)`. Если срабатываний
      много — проблема в поиске, а не в модели: он отдаёт одно и то же.
- [ ] **C4. Разнообразие тем.** Канал не должен писать про одно и то же
      каждую неделю. Проверить на длинной дистанции по одному каналу.
- [ ] **C6. Очередь без Telegram может встать молча.** С 27.07 пост без
      доставленной карточки ждёт решения бесконечно — это правильно, но если
      человек не заходит на сайт и не подключает уведомления, посты копятся и
      ничего не происходит. Нужен способ напомнить: письмо, баннер при входе
      или предложение подключить Telegram прямо в очереди.
- [ ] **C7. Настоящий анализ стиля в онбординге.** Экран «Проанализировать
      мой канал» сейчас только запоминает @username. Механизм анализа в
      продукте есть (`/api/analyze_style_only`, работает в настройках канала).
      Включать ли его на первом экране — решение владельца: это лишний вызов
      модели до того, как человек увидел хоть один пост.
- [ ] **C5. Что делать, когда новостей нет.** Сейчас пост просто не
      создаётся. Пользователь этого не видит и не понимает.

### D. Путь к деньгам

- [ ] **D1. Замерить воронку заново.** Все прежние выводы недействительны:
      лендинг показывал белый экран пользователям без VPN. Нужны чистые
      данные за 3–7 дней после фикса.
- [ ] **D2. Момент оплаты.** Понять, что человек видит и чувствует на экране
      тарифов и почему закрывает его. Сейчас `payment_started → success` = 0.
- [ ] **D3. Поговорить с зарегистрировавшимися.** Написать лично каждому, кто
      создал канал и не заплатил. Два ответа информативнее месяца тестов.
- [ ] **D4. Определиться с сегментом.** «Для авторов, экспертов, SMM и
      агентств» — это ни для кого. Гипотеза: агентство с десятью каналами
      платит охотнее любителя с одним. Проверяется в D3.
- [ ] **D5. Удержание.** Что заставит продлить подписку на второй месяц.
      Сейчас об этом не думали вообще.

### E. Надёжность и эксплуатация

- [ ] **E1. Ежедневный quality-scan в деле.** Джоба есть; проверить, что
      уведомления доходят и находки не мусорные.
- [ ] **E2. Что видит пользователь при сбое ИИ.** Провайдер недоступен,
      кончились токены, тема отклонена — каждый случай должен быть
      объяснён по-человечески.
- [ ] **E3. Разделить чужой проект в биллинге.** В счёте Yandex Cloud
      смешаны AutoPost и другая платформа — себестоимость считается неточно.
- [ ] **E4. Убрать лишние вызовы моделей.** В биллинге есть Alice, YandexGPT,
      Qwen — похоже, диагностический `llm_compare` работает сам по себе.

---

## 3. Как работает автономная сессия

1. Прочитать этот файл и `CLAUDE.md`.
2. Взять **одну** задачу: сначала из A, если там есть незакрытые; иначе из B,
   затем C, D, E.
3. Сделать, проверить на реальных данных или в браузере, закоммитить, открыть PR.
4. Дописать запись в журнал (свежие сверху) и отметить задачу.
5. Если по ходу нашлась новая проблема — добавить в бэклог, а не чинить всё
   сразу (принцип 5).

Запись в журнале: что сделано, **как проверено**, что осталось незакрытым.
Без «улучшил» и «оптимизировал» — конкретно.

---

## 4. Журнал работ

### 2026-07-28 — в юридические ссылки нельзя было попасть пальцем (B13)

- Замер до правки (390×844). Кабинет: «Оферта» 44×19, «Политика
  конфиденциальности» 185×19, «Возврат» 48×19. Лендинг: 48×20, 201×20, 52×20.
  Рекомендованный минимум — 44×44, то есть по высоте не хватало больше
  чем вдвое.
- Чинится тем же приёмом, что и кнопка «⋯» на прошлой неделе: не увеличением
  шрифта, а отступами. `padding: 13px 8px` в кабинете, `12px 9px` на лендинге.
  Стало 45px и 44px по высоте.
- Чтобы надписи не разъехались вдвое, `gap` разделён: `column-gap: 0` съедает
  ровно те 8px и 9px, что добавил padding, `row-gap: 4px` остаётся. Замер
  после правки: текст «Оферта» стоит на тех же 41px в кабинете и на тех же
  114px на лендинге, что и до правки. Высота подвала 120→121px и 130→150px.
- Проверено результатом, а не кодом. `elementFromPoint` в четырёх точках
  каждой ссылки (4px от верха, низа, левого и правого края) — все 12 точек
  на 390px и все 12 на 1280px попадают в ссылку, а не в подвал. Настоящий тап
  в 4px от верхнего края (там, где до правки была пустота) ведёт на
  `/legal/offer`, `/legal/privacy`, `/legal/refund` — проверено кликом.
  Горизонтального переполнения нет ни на одной из четырёх комбинаций.
- Две находки по дороге, обе исправлены здесь же:
  - После отступов «© АвтоПост» и «Оферта» на лендинге стояли в одной строке,
    но по вертикали разъехались на 12px: у `<span>` отступов нет, а flex по
    умолчанию растягивает элементы. Добавлен `align-items: center` в обоих
    подвалах — центры совпали.
  - `landing.css` подключался без версии в адресе, а `Cache-Control` сервер
    не шлёт. Правка стилей дошла бы только до новых посетителей. Добавлено
    `?v=20260728d`.
- Ошибка в собственной проверке, чтобы не повторить: первый вариант скрипта
  мерил координаты сразу после `scrollIntoView()`, а на лендинге включён
  `scroll-behavior: smooth` — все 12 точек показали `null`, и это выглядело
  как «ссылки не нажимаются вообще». Прокрутка и замер должны быть разными
  шагами с паузой.

### 2026-07-28 — три четверти первого экрана уходили на объяснения (B7)

- Плотность мерили не на глаз, а числом: **на каком пикселе начинается первый
  пост** — то, ради чего на экран и заходят.
- Замер до правки (390×844): первый пост начинался на **612px**, то есть 73%
  первого экрана занимали шапка канала (208px) и блок очереди (163px).
  До первого поста человек прочитывал 51 слово. На телефоне с адресной
  строкой (полезная высота ~700px) пост оказывался у самого края.
- Причина в блоке очереди: он объяснял механику («пост ждёт вашей кнопки или
  таймера на карточке») **на каждом заходе, всегда**. Но как только пост
  появился, его карточка говорит это про себя сама — «сам не опубликуется»
  или обратный отсчёт. Получался третий пересказ одной мысли, и он стоил
  места на самом дорогом экране.
- Сделано: механика показывается только при пустой очереди, когда
  продемонстрировать её не на чем. Полный текст никуда не делся — он под
  «Подробнее», как и был.
- Стало: блок 163px → **121px**, первый пост 612px → **571px**. Виден на
  первом экране и при высоте 700px.
- Проверено на двух состояниях: при пустой очереди объяснение на месте и
  карточек нет; при непустой объяснения нет, а гарантия видна на каждой
  карточке. «Подробнее» в обоих случаях разворачивает полный текст. Ошибок в
  консоли нет. Тесты 93/11, версия кэша `20260728c`.
- Не закрыто: шапка канала (208px) — там аватар, название, @username, статус,
  тема канала и две кнопки. Тема канала не меняется от захода к заходу и
  редактируется в настройках; её можно убрать с рабочего экрана. Не делал:
  это уже вкусовое решение о вёрстке, а не устранение повтора.

### 2026-07-28 — самая мелкая кнопка стояла рядом с необратимым действием (B4)

- Замерили читаемость и попадаемость на 390px по всем внутренним экранам:
  размеры шрифтов, обрезанный многоточием текст, размеры целей под палец и
  зазоры между соседями.
- **Обрезанного текста нет нигде, горизонтального переполнения нет нигде** —
  это отдельный проверенный факт.
- Самая мелкая цель — кнопка «⋯» в карточке поста: **32×27px**. Рядом с ней,
  в 8 пикселях, «Опубликовать сейчас» — то есть промах пальцем стоит поста,
  ушедшего в канал. Рекомендации Apple и Material — 44×44 и 48×48.
- Починено без изменения внешнего вида: у кнопки нет ни фона, ни рамки,
  поэтому увеличенные отступы расширяют только область нажатия. Стало
  **44×40**, площадь выросла вдвое. Проверено, что фон остался прозрачным
  (`rgba(0,0,0,0)`), рамки нет, меню по клику открывается, горизонтального
  переполнения нет.
- Что намеренно не трогали:
  - подписи разделов 11px («ТЕЛЕГРАМ», «О КАНАЛЕ», «СТАРТ») — это заглавные
    с разрядкой, осознанный типографский приём; менять типографику целиком —
    решение о дизайне, а не исправление дефекта;
  - ссылки в подвале («Оферта», «Политика», «Возврат») — 44×19 и 48×19.
    Мелко, но это подвал, а не рабочий экран. Записано ниже как B13.
- Тесты 93/11, версия кэша `20260728b`.

### 2026-07-28 — «проанализируем ваш канал» ничего не анализировало (B2)

- Прошли вторую ветку онбординга — «Проанализировать мой Телеграм-канал».
- Экран обещал: «Сгенерируем пример поста **в стиле вашего канала**». Никакого
  анализа стиля там нет — в самом коде рядом стоит комментарий «не обещаем
  полноценный анализ». То есть обещание было написано вопреки собственной же
  пометке разработчика.
- Хуже: введённый @username подставлялся в поле **темы**. Человек, попросивший
  «пиши в стиле моего канала», получал пост про строку «@durov», а у канала
  навсегда оставалась тема «@durov» — то есть все будущие посты тоже.
- Исправлено. Username больше не становится темой: он запоминается как
  `tg_chat` создаваемого канала (это избавляет от повторного ввода при
  подключении), на экране темы показывается «Канал @mychannel запомнили —
  подключим его после первого поста», а обещание заменено на то, что
  действительно происходит. Добавлен сброс `App._qsChannelHandle` при новом
  заходе в онбординг — иначе канал из прошлой попытки молча приклеился бы к
  следующему.
- Проверено на живом потоке: обещание про стиль исчезло, поле темы пустое,
  подтверждение про канал видно, а в запросе на создание канала
  `tg_chat='@mychannel'` (ссылка `t.me/…` приведена к @username) и
  `about='криптоновости для начинающих'` — тема, а не username.
- Проверка далась не сразу: перехват сети через `route` оставлял запрос
  висеть, и поток обрывался. Записываю рабочий способ на будущее — обернуть
  `window.api` в браузере и смотреть, что приложение реально отправляет.
- Тесты 93/11, версия кэша `20260728a`.
- **Заведено C7:** настоящий анализ стиля в онбординге. Механизм в продукте
  есть (`/api/analyze_style_only`, используется в настройках канала), но
  включать его на первом экране — решение владельца: это лишний вызов модели
  до того, как человек увидел хоть один пост.

### 2026-07-28 — история платежей показывала токены и скрывала сумму (B3)

- Последний непроверенный экран блока B3. Строка выглядела так:
  «27.07.2026, 14:05 · 600 000 ток.» — внутренняя единица учёта на первом
  месте, сокращение, которое нужно расшифровывать, и **ни одного рубля**.
  При этом `p.rub` в данных есть: он использовался для аналитики, но не
  показывался человеку.
- Человек заходит в историю платежей ровно за одним — посмотреть, сколько и
  за что заплатил. Теперь строка: **«490 ₽ · Старт»** первой строкой, ниже
  мелким «26.07.2026, 03:36 · 600 000 токенов». Статус «ожидает» уточнён до
  «ожидает оплаты»: непонятно было, чего именно ждут.
- Название тарифа берём из `App.cfg.packages` — то же место, откуда его
  показывает экран тарифов, поэтому расхождения между ними быть не может.
- Прежний класс `.src-url` не подошёл: у него `white-space:nowrap` и
  `text-overflow:ellipsis`, вторая строка в него не помещалась. Сделана
  своя разметка вместо переиспользования чужой.
- Проверено на двух платежах (оплаченный и ожидающий): суммы, тарифы, даты и
  токены на месте, старое «ток.» исчезло, горизонтального переполнения на
  390px нет (0 px), ошибок в консоли нет. Тесты 93/11, версия кэша
  `20260727n`.
- **B3 закрыта.** Все экраны с числами пройдены: тарифы, очередь,
  «Расширенные», «Настройки», история платежей.

### 2026-07-27 — «тестовый пост» был обычным постом за токены (B3, B5)

- Прошли вкладку «Настройки» тем же способом: сверили подписи с кодом.
- **«Создать тестовый пост» и «Написать пост сейчас» в очереди — это один и
  тот же вызов** `POST /channels/{id}/generate` с одинаковыми параметрами.
  Слово «тестовый» обещало пробный прогон без последствий, а на деле пост
  настоящий: тратит токены и остаётся в очереди (`force_pending=True`, то
  есть таймера у него нет и сам он не опубликуется).
- Исправлено: карточка называется «Проверить настройки», кнопка — «Написать
  пост сейчас», как в очереди, и рядом прямо сказано, что пост обычный,
  тратит токены и встаёт в очередь.
- **Вторая находка — дыра в моей же проверке.** Позавчера я записал, что
  поиск «ты»-форм стал механическим и пропустить ничего нельзя. Это было
  преувеличением дважды: тот поиск брал только формы **с заглавной буквы в
  начале фразы**. Нашлись «нажми Start», «Сначала добавь бота… потом вставь
  @username и нажми „Проверить“», «выбери понравившийся» — четыре штуки.
  Поиск переписан на любые регистр и позицию, все исправлены.
- Не исправлено намеренно: «Сделай живее и разговорнее» в `app.part06.js` —
  это текст промпта, его читает модель.
- Проверено в браузере: новые формулировки на месте, старых нет, ошибок в
  консоли нет. Тесты 93/11, версия кэша `20260727m`.
- Ещё раз наступил на ту же грабку: проверка текста на экране должна быть
  **без учёта регистра**, потому что CSS делает заголовки заглавными. Это уже
  было записано в журнал вчера — и всё равно повторилось.
- Не закрыто по B3: история платежей.

### 2026-07-27 — «Окно публикации» ограничивало не публикацию (B3, принцип 1)

- Шли за B3 (объяснить числа на внутренних экранах) и на вкладке
  «Расширенные» проверили по коду два параметра, а не по их подписям.
- **«Окно публикации (UTC)» ограничивает не публикацию, а написание.**
  `publish_window_start/end` читается в `_is_due()` и в расчёте времени
  следующей генерации (`tasks.py`) — то есть окно решает, когда мы *напишем*
  пост. Когда он выйдет в канал, зависит от подтверждения: пост, написанный
  в 21:50 и подтверждённый утром, выйдет утром, далеко за окном. Подпись при
  этом обещала «Посты публикуются только в это время» — со словом «только».
- Тот же дефект рядом: «Разброс ±N мин» подписан «посты выходят в разное
  время», хотя разброс сдвигает время генерации.
- Исправлено. Заголовок — «Когда писать посты (UTC)», в одном ряду со
  вчерашним «Как часто писать новый пост». Подсказка разная по режимам:
  при автопилоте создание и публикация совпадают («Пишем и публикуем посты
  только в это время»), при подтверждении — «Новые посты пишем только в это
  время. Когда они выйдут в канал — решаете вы».
- Проверено на двух каналах (автопилот и подтверждение): каждый видит свою
  подсказку, старых формулировок не осталось ни на одном, ошибок в консоли
  нет. Тесты 93/11, версия кэша `20260727l`.
- Не закрыто по B3: история платежей и экран тарифов внутри кабинета ещё не
  прошли такую же сверку.

### 2026-07-27 — «Телеграм» кириллицей во всём интерфейсе (B5)

- Вопрос, оставленный владельцу в прошлом цикле, решён им: в русском тексте
  «Телеграм» лучше, чем «Telegram». Лендинг так писал и раньше — привели к
  нему остальное.
- Заменено 37 мест: кабинет, `index.html`, инструкция `how-to.html`, тексты
  ошибок входа в `main.py` и приветствие бота в `tasks.py`.
- Что **не** трогали и почему:
  - идентификаторы кода — `window.Telegram`, `openTelegramLink`,
    `initTelegram`, `loadTelegramSdkAsync`: это API, а не текст;
  - системные промпты в `generator.py` — их читает модель, а не человек, и
    менять формулировки промпта ради типографики рискованно;
  - `"operatingSystem": "Web, Telegram"` и «(Telegram)» в meta-описании
    лендинга — машинные значения и подстраховка для поиска;
  - оферта (`static/legal/offer.html`, 3 упоминания) — юридический документ,
    правка за владельцем.
- Проверено в браузере: на экране входа, на экране канала и в настройках
  латиницы не осталось вовсе, заголовок вкладки «Автопост — ИИ ведёт ваш
  Телеграм-канал», ошибок в консоли нет. Отдельно сверили, что все
  идентификаторы кода целы. Тесты 93/11, версия кэша `20260727k`.
- Не закрыто: оферта. Скажите — поменяю там же одним движением.

### 2026-07-27 — у одного параметра было три имени (B5, термины)

- Проверяли остаток B5 — словарь терминов. Две пары из трёх оказались
  ложной тревогой, и это тоже результат:
  - «Отклонить» и «Удалить» — **разные действия**: `reject` ставит статус
    `rejected` (пост остаётся в истории), `delete` удаляет строку. У
    опубликованного поста написано «Удалить из списка» — честно
    предупреждает, что из Telegram он не исчезнет. Менять нечего.
  - «Telegram-канал» в кабинете употребляется единообразно, 7 из 7.
  - «Публикация» почти везде означает действие («право публикации», «окно
    публикации», «ошибка публикации») — это не синоним слова «пост».
- Настоящая находка: **один и тот же параметр `interval_hours` назывался
  тремя именами** — «Частота генерации» (форма нового канала), «Частота
  публикаций» (онбординг), «Расписание» (расширенные настройки).
- Хуже того, «Частота публикаций» ещё и неверна по сути. По расписанию мы
  **пишем** пост; выйдет он только после подтверждения — а без подключённых
  уведомлений (см. вчерашнюю правку) не выйдет вовсе. Тот же дефект был на
  карточке типа канала: «Публикует по расписанию».
- Свели к паре, где название прямо говорит, что произойдёт:
  **«Как часто писать новый пост»** и **«Как часто проверять новости»**.
  Подписи под типами канала: «Пишем по расписанию» и «Пишем, только если
  есть новость».
- Проверено на трёх экранах (новый канал, расширенные настройки, и там же
  после переключения на новостной тип): новые формулировки на месте, ни
  одного старого варианта не осталось, ошибок в консоли нет. Тесты 93/11,
  версия кэша `20260727j`.
- Заодно поймали свою ошибку в проверке: первый прогон сказал, что надписей
  нет вообще. Причина — CSS делает их заглавными, а сравнение было
  регистрозависимым. Проверять текст на экране надо без учёта регистра.
- Не закрыто: лендинг пишет «Телеграм-канал» кириллицей, кабинет —
  «Telegram-канал» латиницей. Это ближе к позиционированию, чем к словарю,
  поэтому решение за владельцем.

### 2026-07-27 — заголовок блока очереди перестал пересказывать шапку (B10)

- Пересчитали повторы на живом экране (канал с подключённым Telegram, два
  поста — с таймером и без). Одна и та же мысль звучала пять раз, и три из
  них — подряд, в четырёх строках: «Вы подтверждаете каждый пост» (шапка),
  «Вы видите каждый пост до публикации» (заголовок блока) и текст самого
  блока.
- Разобрали, что чем занято. Шапка — индикатор режима, отличает от
  «Автоматическая публикация», и та же строка есть на дашборде. Текст блока
  объясняет механику: кнопка или таймер. Заголовок блока не добавлял ничего —
  он просто пересказывал шапку другими словами.
- Заменили заголовок на то, чего в нём не было: **«В очереди 2 из 3»**.
  Счётчик переехал из середины абзаца в заголовок, где его видно, не читая
  текст. Абзац стал на строку короче.
- Оставшиеся упоминания работают по-разному и остаются: шапка — режим, текст
  блока — механика, карточка поста — состояние конкретного поста («сам не
  опубликуется» или отсчёт). Это не повтор, а три разных уровня.
- Проверено на экране с двумя постами: заголовок показывает счётчик, старый
  заголовок исчез, срок генерации на месте, ошибок в консоли нет. Тесты
  93/11, версия кэша `20260727i`.
- Не закрыто: B7 (общая плотность экрана) — там задача шире, чем повторы.

### 2026-07-27 — таймер публикации идёт только если мы предупредили (B12)

- Владелец попросил решить найденное расхождение «оптимально и максимально
  понятно», то есть отдал решение нам.
- Суть расхождения. `PostApproval` с дедлайном заводился **всегда**, а
  карточка в Telegram уходила только тем, у кого подключены уведомления. У
  остальных пост публиковался через 30 минут, а человек об этом не узнавал
  ниоткуда, кроме сайта, куда мог и не зайти.
- Решение взяли не из вкуса, а из правила 4 в `CLAUDE.md`: публикация по
  таймеру допустима, только если таймер **явно показан**. Не показан —
  значит, публиковать нельзя. Отсюда: **нет доставленной карточки — нет
  таймера**, пост ждёт решения сколько угодно, как написанный вручную.
- Почему не «запускать отсчёт, когда человек открыл очередь»: это выглядит
  аккуратнее, но хуже по сути — зашёл ночью на минуту, таймер пошёл, пост
  ушёл во сне. Простое правило понятнее и безопаснее.
- Цена решения названа честно: у человека без Telegram очередь не публикуется
  сама. Это осознанный размен — молчащая очередь лучше поста, ушедшего к
  чужим подписчикам без ведома владельца. Кому нужна автоматика без
  подтверждений — включает автопилот.
- Интерфейс приведён к тому же: блок очереди, «Подробнее» и описание тумблера
  теперь говорят разное для подключённых и неподключённых, FAQ на лендинге
  переписан. Карточку поста править не пришлось — она и так рисует отсчёт
  только при наличии `approval_deadline`.
- Проверено. Четыре теста: карточка доставлена — таймер есть; нет Telegram —
  таймера нет и в Telegram не ходим; доставка провалилась — таймера нет; пост
  при этом остаётся в очереди. Мутация «завести таймер как раньше» роняет
  профильный тест. В браузере проверены оба состояния пользователя. Всего
  93 прошло, 11 пропущено, версия кэша `20260727h`.
- Не закрыто: у постов, оставшихся без таймера, нет напоминания. Если человек
  не заходит на сайт и не подключает Telegram, очередь просто стоит. Завели
  как C6.

### 2026-07-27 — лендинг обещал ежемесячную цену при разовой оплате (B11)

- Продолжили сверку обещаний с кодом, теперь на лендинге: выписали весь
  видимый текст и проверили каждое утверждение.
- **Нашли расхождение про деньги.** На карточках тарифов стояло «490 ₽/мес»,
  «990 ₽/мес» и так далее, жёстко в HTML. Но `SUBSCRIPTION_ENABLED=false`:
  оплата разовая, никакого «в месяц» не существует — человек платит один раз
  и получает объём постов. Кабинет это давно учитывает и показывает «/мес»
  только по флагу, лендинг показывал всегда.
- Починено тем же способом, что уже принят в кабинете: «/мес» вынесено в
  `<span class="per-month" hidden>` и раскрывается в том же обработчике
  `/api/config`, который раньше показывал только абзац про подписку. Решение
  владельца о модели оплаты не меняется — лендинг просто следует флагу.
- **Гипотезу проверили и опровергли:** думали, что абзац «Тарифы работают как
  подписка: списывается автоматически раз в 30 дней» висит всегда. Нет, он
  уже был `hidden` и показывался по флагу. Врала только цена.
- Проверено запуском в обоих режимах: при `false` — «490 ₽» без периода и
  абзац скрыт; при `true` — «490 ₽/мес» и абзац виден. Ошибок в консоли нет.
  Тесты 89/11. Строку «30 000 ₽/мес» у SMM-специалиста не трогали: там период
  настоящий, и это не наша цена.
- **Завели B12 и не стали чинить сгоряча.** FAQ отвечает «каждый пост сначала
  приходит вам в личку», но карточка уходит только при подключённых
  уведомлениях, а таймер на 30 минут идёт всегда. Для человека без Telegram
  пост опубликуется без единого сообщения ему. Это не правка текста: похоже,
  сама механика таймера требует решения владельца.

### 2026-07-27 — сверили все обещания интерфейса с кодом (принцип 1)

- После вчерашней находки (ложное обещание в блоке очереди) прошли по этому
  же принципу системно: выбрали из интерфейса **все** утверждения вида «мы
  сами», «автоматически», «появятся», «через N минут», «раз в N» — 23 штуки —
  и каждое проверили по коду, а не по соседнему тексту.
- Нашлось одно ложное: на карточке канала **на паузе** стояло «Очередь пуста
  — посты скоро появятся», строкой ниже собственного статуса «На паузе».
  Планировщик берёт каналы с `enabled == True` (`tick`, `_refill_if_active`),
  на паузе не создаётся ничего. Это тот же класс, что уже описан в
  `CLAUDE.md` как случившийся однажды.
- Починено: на паузе карточка говорит «Новые посты не создаются». Про саму
  паузу не повторяем — она уже написана строкой выше.
- **Одну свою гипотезу опровергли по дороге:** думали, что то же обещание
  видно и на неподключённом канале, но `renderChanCard` для него возвращается
  раньше, с блоком «Канал ещё не подключён». Там всё честно.
- Остальные 22 утверждения подтвердились по коду: очередь дозаполняется
  после публикации (`auto-refill after publish`), таймер заводится всегда,
  разовая оплата не списывает автоматически при `SUBSCRIPTION_ENABLED=false`.
- Проверено на двух каналах рядом: на паузе — новый текст, работающий —
  прежний, ошибок в консоли нет. Тесты 89/11, версия кэша `20260727g`.
- Не закрыто: обещания на лендинге и в письмах этим проходом не проверялись —
  только кабинет.

### 2026-07-27 — на виду стояло обещание, которого система не выполняет

- Шли за B10 (одна мысль, сказанная четырьмя способами), но сначала проверили,
  правдива ли каждая из четырёх формулировок. Одна оказалась ложной.
- В блоке очереди постоянно висело: **«Ни один пост не попадёт в канал, пока
  вы не нажмёте „Опубликовать“»**. А в свёрнутом «Подробнее» рядом написано
  обратное: «если вы не отреагируете за 30 мин, мы опубликуем его сами».
- Проверили по коду, а не по текстам. `needs_approval` в `tasks.py` заводит
  `PostApproval` с дедлайном для **каждого** поста, написанного по расписанию,
  причём независимо от Telegram (там отдельный комментарий про этот же класс
  бага). `_auto_publish_after_timeout` по истечении дедлайна публикует пост.
  То есть для основного сценария обещание было неверным, а правда пряталась
  под «Подробнее».
- Заменили на то, что человек и так видит на карточках: «Пост ждёт вашей
  кнопки „Опубликовать“ — или таймера на карточке, если мы написали его по
  расписанию». Заголовок блока — «Вы видите каждый пост до публикации».
  Гарантия не ослаблена: молча по-прежнему не уходит ничего.
- Проверено на двух постах рядом: один с `PostApproval` (карточка показывает
  «⏱ через 26:32, если не подтвердите» и «Не отреагируете — опубликуем этот
  пост сами»), другой без (карточка говорит «сам не опубликуется»). Новый
  текст описывает ровно эти два случая. Ошибок в консоли нет, тесты 89/11,
  версия кэша `20260727f`.
- **B10 остаётся открытой.** Мы починили правдивость, а не плотность: та же
  мысль по-прежнему звучит и в шапке, и в заголовке блока, и на карточках.
  Сокращать это надо отдельно и осторожно.

### 2026-07-27 — срок генерации перестал дублироваться (B6, B9)

- Взяли B6 через конкретную находку B9. Засеяли два канала — с пустой
  очередью и с двумя постами — и прошли экран канала на 390px.
- Подтвердилось: «⏱ Следующая генерация в ближайшие минуты» в шапке и «Ещё 3
  мы готовим — обычно это занимает пару минут» в блоке очереди говорят одно и
  то же, строкой ниже.
- Разобрались, какое место лучше. Блок очереди описывает генерацию **во всех**
  состояниях канала и точнее: сколько именно постов готовим, «очередь
  заполнена», «канал на паузе», «мы сами публикуем — раз в 12 часов». Шапка
  даёт только общую фразу. Поэтому строку убрали из шапки экрана канала.
- На дашборде строка осталась намеренно: там блока очереди нет, и это
  единственное место, где видно, когда появится следующий пост.
- Проверено на обоих каналах и на всех трёх вкладках («Очередь»,
  «Настройки», «Расширенные»): упоминаний срока генерации в шапке — ноль,
  на дашборде строка на месте, ошибок в консоли нет. Тесты 89/11, версия
  кэша `20260727e`.
- **Завели B10, но чинить не стали.** На том же экране «пост сам не
  опубликуется» сказано четырьмя разными формулировками подряд. Это ядро
  обещания продукта (правило 4 в `CLAUDE.md`), и сокращать его надо
  отдельной аккуратной правкой, а не заодно.

### 2026-07-27 — «ты» осталось там, куда не дотянулся список слов (B5, B3)

- Шли за B3 («объяснить каждое число»), а нашли собственную недоделку.
- **Про B3 честный результат: числа уже объяснены.** На экране тарифов
  «Осталось 12–24 постов» идёт вместе со строкой «Диапазон зависит от
  сложности: пост с поиском свежих новостей расходует больше, простой —
  меньше»; реферальные «6–10 бесплатных постов» сразу переведены в токены;
  в очереди «0 из 3» объяснено соседним текстом. Отдельной правки не
  потребовалось, задача остаётся открытой только по внутренним экранам.
- Зато выяснилось, что прошлый отчёт «„ты“-форм в кабинете не осталось» был
  неточен. Тот поиск шёл **по списку слов**, который я написал по памяти, и
  всё, чего в списке не было, прошло мимо: «Включи право» (третий пункт той
  самой инструкции, где первые два я поправил), «Опиши идею», «Канал
  подключишь позже», «Объясни как хочешь», «Открой @maintrpost_bot».
- Заменил способ проверки на механический: ищем повелительные формы
  единственного числа (заканчиваются на и/й/ь и **не** на «-те») и окончания
  «-шь». Список слов больше не нужен.
- Два совпадения оставлены осознанно: «Подпишись чтобы не пропустить» —
  это пример подписи, которую владелец пишет своим читателям, а не мы ему;
  «Сделай короче и проще» уходит в промпт модели и пользователю не виден.
- Заодно выправлена форма внутри одного списка: было «1. Открой… 3. Ввести
  реферальный код» — повелительное вперемешку с инфинитивом.
- Проверено: в отдаваемом сервером `app.js` старых формулировок 0, новых по
  одной; на экранах тарифов и «Расширенные» видны новые тексты, ошибок в
  консоли нет. Тесты 89/11, версия кэша `20260727d`.
- Не закрыто: термины («отклонить»/«удалить», «пост»/«публикация») —
  это остаток B5.

### 2026-07-27 — пять тестов перестали быть декорацией (A6)

- В `test_topic_validation.py` каждая функция печатала результат и
  **возвращала список** несовпадений вместо `assert`. Под pytest возвращённое
  значение никто не смотрит, поэтому тесты проходили при любом поведении
  модели — и, что хуже, при полном отсутствии ключей, когда классификатор
  вообще ничего не классифицирует.
- Переписаны на настоящие проверки. Вдобавок весь файл помечен пропуском без
  `RUN_LLM_TESTS=1` — тем же флагом, что `test_quickstart_flow.py`: оба ходят
  в живую модель и стоят денег на каждом прогоне. «Не проверено» и
  «проверено, всё хорошо» теперь выглядят по-разному.
- В тест на дрейф темы добавлена явная проверка `classification_unavailable`:
  если до модели не дозвонились, тест обязан упасть с понятной причиной, а не
  тихо отрапортовать успех, не дойдя до генерации.
- Доказано сравнением, а не рассуждением. Одна и та же команда
  `RUN_LLM_TESTS=1 pytest test_topic_validation.py`: на версии из `main` —
  **5 passed** без единого ключа; на новой — **5 failed** с текстом, из
  которого видно, что до модели не дозвонились. Обычный прогон: было
  5 passed, стало 5 skipped.
- Весь набор: **89 прошло, 11 пропущено** (было 94 и 6 — пять «зелёных»
  переехали в честно пропущенные, новых поломок нет).
- Не закрыто: настоящее поведение модели на граничных темах по-прежнему не
  проверено — для этого нужен прогон с ключами, `RUN_LLM_TESTS=1`. Теперь
  хотя бы видно, что оно не проверено.
- **Блок A снова закрыт полностью.** A1, A2, A4 заблокированы без доступа к
  проду и ЮKassa.

### 2026-07-27 — наш сбой выдавался за плохую тему пользователя (B2)

- Прошли первый вход с чистого аккаунта на 390px: регистрация → «Что сделать
  сначала?» → тема → «Сгенерировать пост». Путь короткий, лишних вопросов нет.
- На последнем шаге упёрлись в то, что видит пользователь, когда модель
  недоступна: тост «Не удалось проверить тему. Попробуйте переформулировать».
  При этом в логе сервера честно стоит «Ошибка авторизации ИИ». То есть мы
  знаем, что лежит **у нас**, а человеку советуем переписать тему. Сколько ни
  переписывай — будет то же самое.
- Это дороже, чем кажется: точка ровно там, где в бэклоге отмечена самая
  дорогая потеря — «зарегистрировались и не получили ни одного поста».
- Разделили два случая, которые раньше давали один статус:
  `classification_failed` — модель ответила невнятно, переформулировать
  осмысленно; `classification_unavailable` — до модели не дозвонились,
  сообщение прямо говорит, что это на нашей стороне и надо подождать. Оба
  остаются блокирующими: непроверенную тему не пропускаем.
- Попутно закрыли следствие: ветка удаления черновика канала срабатывала и
  при нашем сбое. Человек создавал канал, у нас падал провайдер — и канал
  молча исчезал. Теперь при `classification_unavailable` не удаляем ничего.
- Проверено: ответ API поменялся (`classification_unavailable` и честный
  текст), в браузере тост показывает его же, тема остаётся в поле. Шесть
  новых тестов; обе мутации (вернуть общий статус; вернуть удаление канала)
  роняют ровно по одному тесту. Всего 94 прошло, 6 пропущено.
- **Нашли неприятное и завели как A6:** пять тестов в
  `test_topic_validation.py` не проверяют ничего — их функции возвращают
  список вместо `assert`, поэтому проходят всегда, даже без ключей модели.
  Значит, в «94 прошло» пять штук — пустые. Не чинили: одно изменение за цикл.
- Не закрыто по B2: вторая ветка онбординга («Проанализировать мой
  Telegram-канал») и успешный путь целиком — без ключей модели здесь не
  воспроизводится.

### 2026-07-27 — онбординг перестал повторяться дважды (B1, B8)

- Проверяли B1 — пустые состояния. Засеяли три аккаунта: без каналов; канал
  без подключения к Telegram; канал подключён, но постов ещё нет. Сняли
  экраны на 390px.
- **Сами пустые состояния оказались в порядке** — это отдельный результат, а
  не отсутствие проверки. Канал без подключения: «В очереди 0 из 3. Сами по
  расписанию посты начнут появляться после подключения канала. А написать
  пост можно прямо сейчас». Подключённый без постов: «Ещё 3 мы готовим —
  обычно это занимает пару минут». Оба отвечают на «почему пусто», «что
  будет» и «что сделать сейчас». Задача закрыта.
- Зато нашлось другое, и на самом дорогом экране — первом после регистрации.
  Кнопка «Пропустить» стояла **дважды**: сверху слева и под кнопками выбора,
  обе вели в один и тот же `qsSkip()`. Верхняя вдобавок притворялась
  навигацией: место back-link, а стрелка вправо (на остальных экранах там
  «← все каналы»).
- На следующем экране онбординга — то же самое словами: «Канал можно
  подключить позже — сначала покажем пример поста» и сразу под ним «Сейчас
  покажем пример поста. Потом можно будет… подключить канал».
- Убрали верхнюю «Пропустить»; два абзаца свели в один, сохранив
  перечисление настроек: «Сначала покажем пример поста. Канал, стиль, длину
  и расписание настроите потом».
- Проверено в браузере: на экране выбора теперь ровно одна кнопка
  «Пропустить» (посчитано, а не на глаз), старый второй абзац исчез, кнопка
  «Пропустить» по-прежнему уводит на дашборд, ошибок в консоли нет.
- Тесты: 88 прошло, 6 пропущено. Версия кэша `20260727c`.
- Не закрыто: на экране подключённого канала «Следующая генерация в
  ближайшие минуты» и «Ещё 3 мы готовим — обычно это занимает пару минут»
  говорят одно и то же в двух местах. Завели как B9.

### 2026-07-27 — плашка cookies закрывала юридические ссылки (B4)

- Замеряли мобильную вёрстку на 390px не глазами, а из браузера: считали
  горизонтальное переполнение и пересечения интерактивных элементов с
  фиксированной плашкой внизу.
- Первый замер не нашёл ничего — и это было ложное спокойствие: плашка
  cookies создаётся как голый `div` с инлайновыми стилями, без класса и id,
  поэтому селектор её не находил, а пустой результат выглядел как «всё
  хорошо». Переписали поиск по вычисленному стилю (`position:fixed`,
  прижат к низу).
- После этого нашлось одинаковое на всех экранах: плашка **целиком**
  закрывала ссылки «Оферта», «Политика конфиденциальности» и «Возврат» —
  перекрытие 19px из 19. Прокрутить дальше нельзя, страница кончилась. То
  есть до нажатия «Понятно» юридические документы были недоступны с
  телефона вовсе — при том, что сама плашка предлагает прочитать
  «Подробнее».
- Починка: пока плашка на экране, `body` получает отступ снизу по её
  фактической высоте (на узком экране текст переносится и высота меняется,
  поэтому считаем по `offsetHeight`, а не константой). После «Понятно»
  отступ снимается.
- Проверено замером и кликом: перекрытие было +19px, стало −28px (ссылка
  выше плашки), по «Оферте» кликается, «Понятно» закрывает плашку и убирает
  отступ, после перезагрузки не возвращается. На 1280px поведение то же.
- Горизонтального переполнения на 390px не нашлось ни на одном экране —
  это отдельный проверенный факт, а не отсутствие проверки.
- Тесты: 88 прошло, 6 пропущено. Версия кэша поднята до `20260727b`.
- Не закрыто: B4 шире — смотрели геометрию, а не читаемость внутренних
  экранов (очередь, настройки канала, тарифы).

### 2026-07-27 — интерфейс перестал обращаться то на «ты», то на «вы» (B5)

- Первый цикл в блоке B. Прошли кабинет глазами: подняли локальный сервер,
  засеяли три состояния аккаунта (только что зарегистрировался; канал без
  постов; канал с постами) и сняли экраны на 390px и 1280px.
- Нашли то, что видно сразу: на дашборде «Твои каналы… тебе только выбирать»
  и строкой ниже «Вы подтверждаете каждый пост». На экране входа — обе формы
  в одном предложении: «для твоего Telegram-канала — … с вашим
  подтверждением». Это первый экран, который видит человек из рекламы.
- Посчитали, а не решили на вкус: в кабинете 23 «вы»-формы против 5 «ты»,
  лендинг целиком на «вы», оферта тоже. То есть «ты» — выбросы, и приведение
  к «вы» убирает дефект, а не меняет тон продукта.
- Исправлено 18 мест в 10 файлах, включая сообщения об ошибках («Проверь
  баланс» → «Проверьте баланс») и заголовок вкладки браузера. «ты»-форм в
  кабинете не осталось — проверено поиском по всем `app.part*.js`.
- Проверено в браузере, а не по диффу: сняли те же экраны заново, дашборд и
  вход теперь согласованы. Версия кэша поднята до `20260727a`.
- Тесты: 88 прошло, 6 пропущено. `node --check app.js` проходит.
- Не закрыто: сама задача B5 шире — термины («отклонить» и «удалить»,
  «канал» и «Telegram-канал») по-прежнему вперемешку.
- Попутно нашли и завели B8: на первом экране онбординга одна и та же мысль
  написана дважды подряд. Не чинили — одно изменение за цикл.

### 2026-07-27 — дедупликация покрыта тестами, блок A закрыт (A5)

- Последний непокрытый кусок A5. Порог `DUPLICATE_THRESHOLD` был подобран на
  одной паре постов с прода, и любое изменение `_content_words`, `_STEM_LEN`
  или самого порога проходило незамеченным.
- `test_duplicate_posts.py`, 10 тестов. Проверяется не только детектор, но и
  поведение генерации: близнец вызывает ровно одну перегенерацию, а если и
  она про то же событие — пост не создаётся вовсе. И обратная сторона: на
  непохожем тексте лишней перегенерации не запускается (это чужие деньги).
- Тесты держатся на СООТНОШЕНИЯХ, а не на числах с прода: близнецы выше
  порога, разные события той же тематики ниже, разрыв больше двукратного,
  сравнение по основам даёт выигрыш против словоформ. Продовые посты у нас
  не сохранились — тексты в тесте это честно оговаривают как реконструкцию
  того же события.
- Проверено мутациями. `_STEM_LEN=20` (фактически словоформы) — упало 2
  теста. Порог 0.5 — упало 5. Снятие второй проверки на дубль — упал тест
  про пропуск близнеца. Все мутации откачены.
- Прогон целиком: **88 прошло, 6 пропущено** и на SQLite, и на Postgres.
- Дефектов в логике дедупликации не нашлось. Оба падения на старте были
  моими: пост в фикстуре стоял в статусе `queued`, который в сравнение не
  попадает вовсе (берутся `pending/scheduled/published`), и email
  пользователя не был уникальным.
- **Блок A закрыт полностью**: A1, A2, A4 заблокированы без доступа к проду
  и ЮKassa, A3 и A5 сделаны. Следующие циклы — блок B, понятность интерфейса.

### 2026-07-27 — автосписание покрыто тестами (A5, вторая половина)

- Взяли вторую половину A5: защиту от двойного списания. Она ни разу не
  выполнялась — `SUBSCRIPTION_ENABLED=false`, рекуррент на согласовании.
  В день включения автоплатежей этот код заработал бы впервые сразу на
  боевых картах, без единой проверки.
- `test_subscription_billing.py`, 10 тестов. ЮKassa подменена, наружу не
  уходит ни один запрос. Покрыто: начисление токенов ровно один раз;
  повторный запуск джобы на том же периоде; падение процесса после списания,
  но до записи `last_period_key`; неизвестный исход против явного отказа;
  приостановка после `SUBSCRIPTION_MAX_FAILS`; списание зафиксированной цены
  при поднятых тарифах; детерминированность `Idempotence-Key`; подписка без
  карты; выключенный флаг.
- Проверено мутациями, а не только зелёным прогоном. Убрали проверку уже
  оплаченного периода — упал тест про падение после списания. Заменили
  `sub.price_rub` на текущую цену из конфига — упал тест про фиксацию цены.
  Сняли различие «отказ / неизвестный исход» — упал тест про pending. Каждая
  мутация роняла ровно один тест, все откачены.
- Прогон целиком: **78 прошло, 6 пропущено** и на SQLite, и на Postgres.
- Попутно: `period_no` у подписки начинается с 1, а не с 0 — это было моей
  ошибкой в ожиданиях теста, не дефектом продукта. Дефектов в самой логике
  списания тесты не нашли.
- Не закрыто: дедупликация постов по-прежнему без тестов. Это последний
  пункт A5.

### 2026-07-27 — удаление аккаунта покрыто тестами, найден четвёртый FK (A5)

- Взяли A5 с удаления аккаунта: оно падало в проде три раза, всегда одинаково —
  появлялась таблица с FK на `user.id`, а шаг её очистки дописать забывали.
- Тест сразу нашёл четвёртый случай: **`Subscription.user_id` — настоящий FK,
  а `delete_account()` его не чистил.** Завели его мы сами 26.07 вместе с
  подпиской. Воспроизведено на настоящем Postgres:
  `ForeignKeyViolation: subscription_user_id_fkey`.
- Опаснее, чем выглядит. Шаг 7 при неизвестном FK не отдаёт ошибку, а
  анонимизирует запись и возвращает `{"ok": true}`. То есть удаление
  «получалось», но строка `Subscription` оставалась жива вместе с
  `payment_method_id` — сохранённой картой. `charge_due_subscriptions`
  продолжила бы списывать с карты человека, который удалил аккаунт.
- Сейчас баг спящий: при `SUBSCRIPTION_ENABLED=false` строк в таблице нет.
  Он выстрелил бы в день включения автосписаний — то есть ровно тогда, когда
  появились бы живые деньги.
- Починено: шаг 6.7 удаляет подписки до удаления пользователя. Карту мы не
  храним, а без `payment_method_id` списать нечем.
- Проверено: 5 тестов в `test_account_deletion.py` — полный аккаунт со всеми
  связями, приглашённые пользователи, подписка с картой, пустой аккаунт,
  и сверка схемы со списком очищаемых таблиц. Прогон на обоих движках:
  **68 прошло, 6 пропущено** и на SQLite, и на Postgres. До правки те же
  тесты падали на обоих — это и есть доказательство, что они не пустые.
- `conftest.py` теперь включает `PRAGMA foreign_keys=ON` для SQLite: обычный
  локальный прогон ведёт себя как прод. Прогон на Postgres — через
  `PYTEST_DATABASE_URL` (отдельное имя, чтобы боевой URL нельзя было
  подставить случайно).
- Не закрыто: дедупликация постов и биллинг по-прежнему без тестов.
  Проверка «не забыта ли таблица» намеренно грубая — ищет имя модели в тексте
  функции; ловит забывчивость, но не небрежность.

### 2026-07-27 — тесты снова ловят регрессии (A3)

- Было: `python3 -m pytest -q` → 21 падение, 6 прошло, 42 ошибки. Причин
  ровно две, и обе не в тестах: async-функции не запускались вообще
  (pytest-asyncio в окружении нет), а фикстура `client` не была определена
  нигде. Сами тесты при этом написаны и логику проверяют.
- Появился `conftest.py`. Запуск корутин — свой, десять строк в
  `pytest_pyfunc_call` с одним общим циклом событий: новую зависимость ради
  этого тянуть не стали. Приложение поднимается в процессе через
  `ASGITransport` — ни uvicorn, ни свободного порта, ни `sleep 3`, которых
  требовали инструкции в шапках тестов.
- `DATABASE_URL` присваивается жёстко, во временный каталог, а не через
  `setdefault`. Если в оболочке случайно окажется боевой URL, `setdefault`
  оставил бы его — и тесты начали бы регистрировать пользователей в проде.
- `test_user_events.py` создавал httpx-клиент сам и уходил в реальную сеть
  на localhost:8000. Переведён на общую фабрику `make_client()`.
- `test_quickstart_flow.py` тратит настоящие токены модели на каждый прогон
  (валидация темы + генерация). Пропускается явно, с причиной в выводе
  pytest; запуск при необходимости — `RUN_LLM_TESTS=1`.
- Стало: **63 прошло, 6 пропущено, 0 падений**. Два прогона подряд дают
  одинаковый результат.
- Проверено не «зелёным цветом», а мутациями. Сняли верхний предел
  `period_minutes` — упал `test_6_limit_and_period_validation`. Сломали
  анонимизацию `user_key` — упали `test_2_registration_event_present_no_pii`
  и `test_no_pii_leaked`. Обе мутации откачены, дерево чистое.
- Не закрыто: покрытия нет там, где ошибка дороже всего — удаление аккаунта,
  дедупликация постов, биллинг. Заведено как A5.

### 2026-07-26 — базовая настройка автономной работы

- Созданы `PRODUCT_ROADMAP.md` и `CLAUDE.md` — общая память между сессиями.
- Зафиксировано состояние: тесты не запускаются (нет `conftest.py`, 42 ошибки),
  деплой сломан на стороне Timeweb, автоплатежи на согласовании.
- Проверено: `pytest -q` → 21 failed, 6 passed, 42 errors.

### 2026-07-26 — качество постов и обратная связь

- Посты-близнецы: три причины (генерация двух постов на одной выдаче поиска,
  дедупликация по заголовкам, отсутствие проверки готового текста). Порог
  откалиброван на реальной паре с прода: близнецы 0.32, разные события 0.00–0.04.
- После подключения канала интерфейс молчал — теперь показывает «готовим
  первые посты» и обновляется сам.
- Добавлен `/api/internal/quality-scan` + ежедневная джоба с уведомлением
  в Telegram. Проверено на шести заложенных дефектах — найдены все.

### 2026-07-26 — доступность и экономика

- Убран `@import` на Google Fonts: страница висела 25 секунд у пользователей
  без VPN, то есть у рекламного трафика. Замерено до/после: 25 с → 1.9 с.
- HTML отдаётся с `no-cache` — иначе деплой не доезжал до браузеров.
- Себестоимость поста: **~1.2 ₽**, маржа тарифов ~89%. Прежняя оценка 6.14 ₽
  была завышена впятеро: внутренние единицы учёта считались токенами провайдера.
- Тарифы: 490 / 990 / 2490 / 4990 ₽, объёмы приведены к реальной себестоимости.
- Подписка с автосписанием, отвязкой карты и фиксацией цены; пока выключена
  флагом до согласования с ЮKassa.
